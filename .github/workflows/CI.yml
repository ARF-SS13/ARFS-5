name: BYOND Compile CI
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    container: tgstation/byond:513.1527
    strategy:
      fail-fast: false
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    
    - name: Run Dream Maker
      id: compile
      run: DreamMaker *.dme